import 'package:flutter/material.dart';

// --- Color Palette Definitions ---

// Deep dark background for the Scaffold
const Color darkBackgroundColor = Color(0xFF0D1C3A); 
// Slightly lighter card/container color
const Color cardColor = Color(0xFF1B2C54); 
// Vibrant blue for accents, primary buttons, and focus
const Color accentBlue = Color(0xFF4C7FFF); // A strong, recognizable blue
// High-contrast white for primary text and icons
const Color onPrimaryWhite = Color(0xFFFFFFFF); 
// Secondary color for less important text (e.g., timestamps)
const Color secondaryTextColor = Color(0xFFB0B0B0); 
// Color for the mic glow (can be a variation of the accent)
const Color micGlowColor = Color(0xFF00C0FF); // A bright cyan